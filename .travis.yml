sudo: true
dist: trusty

jobs:
  include:
    - stage: build docker image
      script:
      - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      - docker build -t rstudio .
      - docker images
      - docker tag rstudio $DOCKER_USERNAME/rstudio
      - docker push $DOCKER_USERNAME/rstudio